{
  "name": "sitetalk-server",
  "version": "1.0.0",
  "description": "Realtime backend for the SiteTalk Chrome extension — per-site chat rooms over WebSocket, plus a small REST API for optional accounts.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "ws": "^8.17.1"
  }
}
